# Base path the browser calls for API requests. The Vite dev server proxies this
# path to the backend container (see vite.config.js), so the browser only ever
# talks to one origin and auth cookies stay same-origin without any CORS setup.
VITE_API_BASE_URL=/api
